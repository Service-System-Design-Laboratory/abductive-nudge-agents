# Abductive Dialogue Pipeline — Extended Paper Experiment

> **Empathy-Driven Abductive Reasoning in Multi-Agent Dialogue Systems**
>
> This module implements a reproducible experiment for observing how **explicit
> abductive reasoning** (Observation → Assumption → Hypothesis → Evaluation →
> Selection) contributes to empathetic and creative dialogue, with internal
> ablation across three knowledge-source dimensions: **PKG**, **RAG**, and **Judge**.

---

## 1. Research Questions

| # | Question |
|---|---------|
| RQ1 | How does the explicit abductive pipeline (O/A → Hypothesis → Judge → Decision) shape the **diversity and explanatory quality** of generated hypotheses? |
| RQ2 | What roles do **Personal Knowledge Graphs (PKG)** and **external evidence (RAG)** play in hypothesis grounding and persona alignment? |
| RQ3 | Does **agent-as-a-judge** scoring improve the selection of empathetic, well-grounded responses? |

---

## 2. Core Design: Abductive Reasoning with O/A

### 2.1 Abductive Cycle

The pipeline implements Peirce's abductive inference as a structured agent chain:

```
Observation (O) — facts extracted from user input (cannot be denied)
        ↓
Assumption (A) — tentative premises placed by ContextAgent (revisable)
        ↓
Hypothesis (H) — "best explanation" candidates that explain O under A
        ↓
Prediction (P) — what each H predicts if true
        ↓
Test (T)       — discriminating questions to differentiate Hs
        ↓
Selection      — choose the H with best explanatory fit
        ↓
Response       — empathetic response that includes T to close the loop
```

### 2.2 Design Rules (Per Agent)

| Agent | Key Rules |
|-------|-----------|
| **ContextAgent** | ① O = facts from input only (no interpretation). ② A = tentative + revisable. ③ triggers derive from gaps between O and A. ④ rag_queries target what is unknown. |
| **ExplorerAgent** | ① URL required for every evidence item. ② 0 results is acceptable (fallback continues). ③ source_type classification required. |
| **HypothesisAgent** | ① Each H must declare which O it explains (`explains: [O1, O2]`). ② Each H must declare its assumptions (`assumes: [A1]`). ③ `predictions` = observable consequences if H is true. ④ `discriminating_questions` = questions that differentiate this H from others. ⑤ `type` must be diverse (avoid all same type). |
| **JudgeAgent** | ① Scoring is heuristic, not truth-judgement. ② `fatal_flaws` are hard vetoes. ③ Testability/discriminability should be noted in rationale. |
| **DecisionAgent** | ① Select exactly 1 hypothesis. ② Frame as options for user (2 choices max). ③ Include discriminating question to advance the abductive loop. |
| **DialogueAgent** | ① acknowledgement + reflection (empathy minimum). ② Options + small action (nudge minimum). ③ Embed discriminating question naturally. ④ Never assert — present as possibility. |

---

## 3. Directory Structure

```
newresearch/
├── README.md              ← This file (design doc + progress log)
├── __init__.py
├── config.py              ← RunConfig & global settings
├── schemas.py             ← Pydantic I/O contracts (all agents)
├── scenarios.py           ← 3 scenarios + get_persona() (Neo4J連携)
├── seed_pkg.py            ← Neo4J PKG シードスクリプト
├── neo4j_pkg.py           ← Neo4J PKG リーダー
├── run_A.py               ← Condition A (Full) 実行ファイル
├── run_B.py               ← Condition B (−PKG) 実行ファイル
├── run_C.py               ← Condition C (−RAG) 実行ファイル
├── run_D.py               ← Condition D (−Judge) 実行ファイル
├── agents/
│   ├── __init__.py
│   ├── base.py            ← BaseAgent (LLM call + logging)
│   ├── context.py         ← ContextAgent  — O/A extraction + triggers
│   ├── explorer.py        ← ExplorerAgent — Serper検索 + LLM解釈
│   ├── hypothesis.py      ← HypothesisAgent — Diversified abductive generation
│   ├── judge.py           ← JudgeAgent    — Agent-as-a-Judge (4-axis)
│   ├── decision.py        ← DecisionAgent — Select 1 hypothesis
│   └── dialogue.py        ← DialogueAgent — Empathy + Nudge + Test question
├── prompts/
│   ├── context.txt
│   ├── explorer.txt       ← Serper検索結果のLLM解釈用
│   ├── hypothesis.txt
│   ├── judge.txt
│   ├── decision.txt
│   └── dialogue.txt
├── pipeline.py            ← End-to-end pipeline orchestrator
├── runner.py              ← 12-run ablation executor (統合版)
├── metrics.py             ← Quantitative metric extraction → CSV
├── visualize.py           ← Figure generation (matplotlib)
└── runs/                  ← Output (git-ignored)
    └── {run_id}/
        ├── config.json
        ├── context.json
        ├── evidence.json
        ├── hypotheses.json
        ├── judgements.json
        ├── decision.json
        └── final.json
```

---

## 4. Pipeline Architecture

```
user_input + persona + PKG + config
        │
        ▼
┌──────────────┐
│ ContextAgent │  Extract O (observations), A (assumptions), triggers, rag_queries
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ExplorerAgent │  External evidence via Serper API  [skip if use_rag=false]
└──────┬───────┘
       │
       ▼
┌────────────────┐
│HypothesisAgent │  3–5 hypotheses: explains O, assumes A, predicts P, tests T
└──────┬─────────┘   [PKG seeds diversity if use_pkg=true]
       │
       ▼
┌────────────┐
│ JudgeAgent │  4-axis scoring + fatal_flaws  [skip if use_judge=false]
└──────┬─────┘
       │
       ▼
┌───────────────┐
│ DecisionAgent │  Select 1 H + user_facing_frame (options + question)
└──────┬────────┘
       │
       ▼
┌────────────────┐
│ DialogueAgent  │  Empathy + Nudge + discriminating question
└────────────────┘
       │
       ▼
   final_response + full JSON logs
```

---

## 5. Ablation Conditions

| Condition | `use_pkg` | `use_rag` | `use_judge` | Purpose |
|-----------|-----------|-----------|-------------|---------|
| **A** (Full) | ✓ | ✓ | ✓ | All components active |
| **B** (−PKG) | ✗ | ✓ | ✓ | Observe PKG's role in persona alignment & hypothesis diversity |
| **C** (−RAG) | ✓ | ✗ | ✓ | Observe RAG's role in evidence grounding |
| **D** (−Judge) | ✓ | ✓ | ✗ | Observe Judge's role in hypothesis selection quality |

---

## 6. Scenarios

| ID | Focus | Designed to stress | Input (user_input) |
|----|-------|--------------------|---------------------|
| **S1** | Ambiguity / Information gap | Trigger extraction, multiple plausible Hs | 卒業研究を進めているが、テーマに自信が持てず、このまま大学院に進むべきか、それとも別の道を考えるべきか迷っている。 |
| **S2** | Persona-dependent concern | PKG utilisation, persona_alignment | 最近、仕事でAIツールを導入するプロジェクトを任されたが、周囲の反応が冷たく、自分のやり方が間違っているのか不安になっている。 |
| **S3** | External-knowledge-dependent | RAG evidence quality | 地方に移住して農業をやりたいと漠然と思っているが、収入や生活の現実がわからず踏み出せない。 |

All scenarios use a **single fixed persona** with a pre-defined PKG.

---

## 7. Agent I/O Contracts (JSON)

### 7.1 ContextAgent

```json
{
  "observations": [
    {"id": "O1", "text": "fact extracted from input"}
  ],
  "assumptions": [
    {"id": "A1", "text": "tentative premise", "status": "tentative"}
  ],
  "triggers": [
    {"type": "information_gap | ambiguity | contradiction | focus", "text": "..."}
  ],
  "questions": ["..."],
  "rag_queries": ["..."],
  "constraints": {"tone": "supportive", "safety": "avoid determinism"}
}
```

### 7.2 ExplorerAgent

```json
{
  "evidence_items": [
    {"rank": 1, "title": "...", "snippet": "...", "url": "https://...", "source_type": "edu|gov|org|com|other"}
  ],
  "notes": ""
}
```

### 7.3 HypothesisAgent

```json
{
  "hypotheses": [
    {
      "hypothesis_id": "H1",
      "type": "psychological | environmental | social | skill | value | evidence_gap | goal_mismatch | constraint_conflict | other",
      "explains": ["O1", "O2"],
      "assumes": ["A1"],
      "statement": "...",
      "mechanism": "...",
      "predictions": ["if true, then ..."],
      "discriminating_questions": ["question that differentiates this H from others"],
      "persona_link": ["pkg:node_id"],
      "evidence_link": ["ev:1"]
    }
  ]
}
```

### 7.4 JudgeAgent

```json
{
  "judgements": [
    {
      "hypothesis_id": "H1",
      "scores": {
        "explanatory_power": 4,
        "consistency": 5,
        "persona_alignment": 3,
        "evidence_reliability": 2
      },
      "rationale": "...",
      "fatal_flaws": []
    }
  ]
}
```

### 7.5 DecisionAgent

```json
{
  "selected_hypothesis_id": "H2",
  "selection_rationale": "...",
  "user_facing_frame": {
    "framing": "options",
    "questions": ["どちらが近い感覚ですか？"],
    "options": ["option A description", "option B description"]
  }
}
```

### 7.6 DialogueAgent

```json
{
  "final_response": "...",
  "markers": {
    "empathy": {
      "acknowledgement": true,
      "reflection": true,
      "perspective_prompt": false
    },
    "nudge": {
      "has_question": true,
      "has_options": true,
      "has_small_action": true,
      "directive_level": "low"
    }
  }
}
```

---

## 8. Metrics (Observational — No Statistical Claims)

| Category | Metric | Source |
|----------|--------|-------|
| **Hypothesis Diversity** | `hypothesis_count` | hypotheses.json |
| | `type_diversity` (unique types) | hypotheses.json |
| **Abductive Structure** | `observation_coverage` (% of O explained by selected H) | hypotheses + decision |
| | `prediction_count` (per H) | hypotheses.json |
| | `discriminating_question_count` (per H) | hypotheses.json |
| **Judge Structure** | 4-axis score mean (per hypothesis) | judgements.json |
| | selected vs. non-selected score gap | judgements + decision |
| **Selection Consistency** | `top_total_selected` (0/1) | decision + judgements |
| | `top_persona_selected` (0/1) | decision + judgements |
| **Source Utilisation** | `pkg_ref_count` | hypotheses.json |
| | `evidence_ref_count` | hypotheses.json |
| | `evidence_used_ratio` | hypotheses.json |
| **Empathy / Nudge** | empathy marker rate (3 types) | final.json |
| | nudge marker rate (3 types) | final.json |
| | `directive_level` distribution | final.json |

---

## 9. Planned Figures

| Figure | Type | Content |
|--------|------|---------|
| Fig. A | Bar | Selected vs. non-selected Judge scores (4 axes) |
| Fig. B | Grouped Bar | `type_diversity` by ablation condition |
| Fig. C | Stacked Bar | Empathy/Nudge marker rates by condition |
| Fig. D | Table | Per-run summary (scenario × condition → key metrics) |

---

## 10. Execution

```bash
# Run all 12 ablation experiments
python -m newresearch.runner --all

# Run a single condition
python -m newresearch.runner --scenario S1 --condition A

# Collect metrics → CSV
python -m newresearch.metrics

# Generate figures
python -m newresearch.visualize
```

### Execution Matrix: 3 scenarios × 4 conditions = **12 runs**

---

## 11. Fallback Handling

| Failure | Recovery |
|---------|----------|
| JSON parse error | 1× repair prompt retry |
| 0 evidence items | Continue; cap `evidence_reliability` at 3 |
| Too few hypotheses | 1× regeneration with explicit count constraint |
| Judge score bias | Fixed tie-break order (no manual override) |

---

## 12. Completion Criteria

- [ ] 12 runs completed without error
- [ ] All JSON logs saved under `runs/`
- [ ] `metrics.csv` generated
- [ ] ≥ 2 figures generated (Judge scores + Empathy/Nudge)

---

## 13. Progress Log

| Date | Task | Status | Notes |
|------|------|--------|-------|
| 2026-02-06 | README.md (design doc) | ✅ | O/A abduction pattern, design rules per agent, I/O contracts, metrics定義 |
| 2026-02-06 | schemas.py | ✅ | 15+ pydantic models (Observation, Assumption, Hypothesis, JudgeScores, etc.) |
| 2026-02-06 | config.py | ✅ | RunConfig, ABLATION_CONDITIONS (A/B/C/D), AzureSettings |
| 2026-02-06 | scenarios.py | ✅ | S1/S2/S3 + 固定ペルソナ(田中翔太, 10-node PKG) |
| 2026-02-06 | agents/ (6 agents + prompts) | ✅ | Context, Explorer, Hypothesis, Judge, Decision, Dialogue 全て実装・動作確認済 |
| 2026-02-06 | pipeline.py | ✅ | 6エージェント逐次実行、run_id別ディレクトリにJSONログ保存 |
| 2026-02-06 | runner.py | ✅ | CLI: `--all`(12runs) / `--scenario S1 --condition A` |
| 2026-02-06 | 動作確認 (S1×A) | ✅ | 33秒で完了。O/A参照・predictions・disc_q 全て正常生成 |
| 2026-02-06 | 全12実験実行 | ✅ | 3シナリオ×4条件=12runs、全て成功 (約6分) |
| 2026-02-06 | metrics.py → CSV | ✅ | 12件抽出。H=5, typediv=5 (全条件)。D条件はjudgeスコア空欄(正常) |
| 2026-02-06 | visualize.py → 4 figures | ✅ | Fig A-D を newresearch/figures/ に生成。CJKフォント選択修正済 |

### 実験結果サマリ (2026-02-06 Run)

| Scenario | Cond | H# | TypeDiv | SelScore | ScoreGap | Empathy | Nudge |
|----------|------|----|---------|----------|----------|---------|-------|
| S1 | A (Full) | 5 | 5 | 19 | 3.0 | 3 | 3 |
| S1 | B (−PKG) | 5 | 5 | 17 | 2.5 | 3 | 3 |
| S1 | C (−RAG) | 5 | 5 | 15 | 0.75 | 3 | 3 |
| S1 | D (−Judge) | 5 | 5 | - | - | 2 | 3 |
| S2 | A (Full) | 5 | 5 | 14 | 0.5 | 2 | 3 |
| S2 | B (−PKG) | 5 | 5 | 17 | 2.25 | 3 | 3 |
| S2 | C (−RAG) | 5 | 5 | 16 | 2.0 | 3 | 3 |
| S2 | D (−Judge) | 5 | 5 | - | - | 3 | 3 |
| S3 | A (Full) | 5 | 5 | 18 | 2.75 | 2 | 3 |
| S3 | B (−PKG) | 5 | 5 | 14 | 0.5 | 2 | 3 |
| S3 | C (−RAG) | 5 | 5 | 16 | 1.75 | 3 | 3 |
| S3 | D (−Judge) | 5 | 5 | - | - | 2 | 3 |

### 観察・考察メモ
- **仮説数・type_diversity**: 全条件で H=5, TypeDiv=5。HypothesisAgent のプロンプトが安定して5種類の仮説タイプを出力している
- **ScoreGap**: Full条件(A)のS1,S3で高いスコアギャップ → Judgeが有効に機能し、明確な差別化選択ができている
- **Empathy**: Condition D (−Judge) で empathy=2 に下がるケースが多い → Judge無しでDecisionの質が下がり、DialogueAgent への入力情報が減少した可能性
- **Nudge**: 全条件で nudge=3 → DialogueAgent のプロンプト制約が効いている
- **次のステップ**: 試行回数を増やした統計的検証 (各条件×3-5回)、discriminating_questions の質的分析

### アーキテクチャ修正 (2026-02-06 後半)

| Date | Task | Status | Notes |
|------|------|--------|-------|
| 2026-02-06 | Neo4J PKG 接続 | ✅ | seed_pkg.py で `:ExpUser` + `:PKGNode` + `:PKG_EDGE` をシード。neo4j_pkg.py で `load_persona_from_neo4j()` を実装。scenarios.py のハードコードPKGを除去 |
| 2026-02-06 | 4条件別実行ファイル | ✅ | run_A.py / run_B.py / run_C.py / run_D.py — 各条件を固定し3シナリオ実行。`python -m newresearch.run_A --scenario S1` で単一シナリオも可 |
| 2026-02-06 | Explorer LLM解釈 | ✅ | ExplorerAgent を Serper直接パース → Serper + LLM解釈 に変更。prompts/explorer.txt を実際に使用するように修正 |
| 2026-02-06 | 動作確認 (Neo4J版) | ✅ | S1×A で Neo4J からペルソナ読込 + LLM付きExplorer の動作確認済 |

#### 修正の要点

1. **PKG → Neo4J**: ペルソナデータ (田中翔太, 10ノード, 7エッジ) を Neo4J 5.15.0 に格納。`bolt://localhost:7687` 経由で実行時にロード。ハードコードの `PERSONA` 定数を `get_persona()` 関数に置換。
2. **4条件分離**: `runner.py --all` の一括実行に加え、条件ごとの独立ファイルにより実行の透明性を確保。
3. **Explorer改修**: 従来はSerper APIの結果をそのままパースしていたが、LLM (`_call_llm_json`) を通して検索結果の解釈・要約・関連性判定を行うように変更。`prompts/explorer.txt` が実際に使用されるようになった。
